# Production environment configuration for nginx deployment
# This file is used when building the React app for production deployment behind nginx

# Set the public URL to /store for correct asset path handling when served from nginx
PUBLIC_URL=/store

# Use empty string for relative API calls - will be proxied by nginx to backend
REACT_APP_API_URL=

# Production port (not used in build, but kept for consistency)
PORT=3000