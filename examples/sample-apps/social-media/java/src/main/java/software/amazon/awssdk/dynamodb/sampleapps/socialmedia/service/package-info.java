/**
 * Domain services encapsulating business rules (visibility, participant caps, validation) and
 * orchestrating cross-table writes. Populated as features evolve in later phases.
 */
package software.amazon.awssdk.dynamodb.sampleapps.socialmedia.service;
