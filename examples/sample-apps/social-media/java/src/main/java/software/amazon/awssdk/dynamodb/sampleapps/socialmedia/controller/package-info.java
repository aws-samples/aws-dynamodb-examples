/**
 * HTTP controllers exposing the {@code /api/v1} routes. Controllers return async results and read
 * the actor from the {@code X-User-Id} header. Populated as features evolve in later phases.
 */
package software.amazon.awssdk.dynamodb.sampleapps.socialmedia.controller;
